# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

# This file is not used for a purely static UI deployment,
# but can be kept for future reference if a backend is re-introduced.
# Settings below are commented out as they are not currently applicable.

# runConfig:
#   # Increase this value if you'd like to automatically spin up
#   # more instances in response to increased traffic.
#   maxInstances: 2
#   minInstances: 0
#   concurrency: 80
# cpu: 0.5
# memoryMiB: 512
# timeoutSeconds: 60
